/**
 * basic.test.ts
 *
 * Simple sanity test to confirm Jest is working.
 *
 * This test should always pass.
 * If this fails, the test environment itself is broken.
 */

test("basic math works", () => {
  expect(2 + 2).toBe(4);
});